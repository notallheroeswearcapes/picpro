export interface Metadata {
    format: string,
    size: number,
    width: number,
    height: number,
    space: string,
    channels: number,
    depth: string,
    density: number,
    chromaSubsampling: string,
    isProgressive: boolean,
    hasProfile: boolean,
    hasAlpha: boolean
}